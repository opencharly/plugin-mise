# 2026.242.1251 — feat: add the mise builder + verb plugin (builder:mise, verb:mise)

## Summary

Adds the charly **mise** plugin — full mise (jdx/mise) support per PLAN-mise.md (umbrella root). 21 files:

- **candy/plugin-mise/** (the plugin): `charly.yml` (plugin: block, `builder:mise` + `verb:mise` providers, `primary: {mise: command}`, ADE plan), `plugin.go` (NewProvider + NewMeta), `provider.go` (Invoke dispatch: OpResolve/OpEmit/OpExecute), `mise_builder.go` (OpResolve → `spec.BuilderResolveReply`: distro-aware base, pinned mise tarball with Go-style arch mapping + `strip-components=1`, staged MISE_DATA_DIR + shims + `/etc/mise/config.toml`; full mode copies the candy's `mise.toml` and runs `mise install` in-stage), `mise_verb.go` (OpEmit → act-script `mise <command>` shell; OpExecute → runs on the venue via the check-engine reverse channel), `schema/mise.cue` (self-contained #MiseInput), `cmd/serve/main.go` (sdk.Serve), go.mod/go.sum, 3 test files (8 tests).
- **candy/mise-consumer/** (the R10 bed consumer): selects `external_builder: mise`, sets `MISE_DATA_DIR` + `path_append: /usr/local/share/mise/shims`, runs a `mise:` verb step (`mise use node@22`), and checks mise + node resolve via shims.
- **charly.yml** (root): project `discover:`, `defaults:` (platform/tag/registry/build), the `check-mise` bed image, and the `check-mise-pod` disposable pod bed.
- **.github/workflows/**: the org-wide `pr-validator.yml` + `tag-on-merge.yml` dispatchers and the `deploy.yml` candy-manifest gate (pins charly v2026.242.1139).
- **.gitignore/.containerignore/.dockerignore/README.md**: repo hygiene.

## How tested

```
$ go build ./... && go vet ./... && golangci-lint run ./...
BUILD_OK / VET_OK / 0 issues

$ go test ./... -v
=== RUN   TestNewMeta_SchemaSplices
--- PASS: TestNewMeta_SchemaSplices (0.00s)
=== RUN   TestRenderMiseStage_Minimal
--- PASS: TestRenderMiseStage_Minimal (0.00s)
=== RUN   TestRenderMiseStage_Full
--- PASS: TestRenderMiseStage_Full (0.00s)
=== RUN   TestInvokeResolve_ReplyShape
--- PASS: TestInvokeResolve_ReplyShape (0.00s)
=== RUN   TestRenderMiseShell_Install
--- PASS: TestRenderMiseShell_Install (0.00s)
=== RUN   TestRenderMiseShell_ToolShorthand
--- PASS: TestRenderMiseShell_ToolShorthand (0.00s)
=== RUN   TestRenderMiseShell_Task
--- PASS: TestRenderMiseShell_Task (0.00s)
=== RUN   TestInvokeEmit_ActScript
--- PASS: TestInvokeEmit_ActScript (0.00s)
PASS
ok  	github.com/opencharly/plugin-mise/candy/plugin-mise	0.004s
```

**R10 bed — `charly check run check-mise-pod` (disposable: true, fresh rebuild):**

```
charly check run check-mise-pod: [image-build] PASS after 28.313s
charly check run check-mise-pod: [check-image] PASS after 19.079s
charly check run check-mise-pod: [deploy-add] PASS
charly check run check-mise-pod: [config] PASS
charly check run check-mise-pod: [start] PASS
charly check run check-mise-pod: [bring-up-members] PASS
charly check run check-mise-pod: [check-live] PASS after 7.925s
charly check run check-mise-pod: [feature-run] PASS
charly check run check-mise-pod: [update] PASS after 14.997s   (fresh rebuild)
charly check run check-mise-pod: [check-live-rebuild] PASS
charly check run check-mise-pod: [feature-run-rebuild] PASS
charly check run check-mise-pod: [cleanup] PASS
charly check run check-mise-pod: [cleanup-members] PASS
charly check run check-mise-pod: PASS (steps=13)
```

Check-live evidence (the builder + verb legs proven live):

```
PASS  check the mise binary provisioned by the builder stage is on PATH  [mise-on-path]  exit=0
PASS  check node installed by the mise verb resolves via the shims  [node-via-shims]  exit=0
PASS  check the mise binary provisioned by the builder stage is on PATH in the running pod  [pod-mise-on-path]  exit=0
PASS  check node installed by the mise verb resolves via the shims in the running pod  [pod-node-via-shims]  exit=0
5 steps: 4 passed, 0 failed, 1 skipped
```

## Rulebook compliance

- **R0 — skills honored:** loaded /charly-internals:plugin, /charly-image:layer, /charly-internals:git-workflow, /charly-check:check before authoring; the plugin mirrors the reference builder/verb candies (plugin-example-builder, plugin-builder-pixi, plugin-cdp).
- **R1 — RCA on every failure, zero warnings:** the bed run surfaced 4 real defects (literal `\n` in the stage RUN, missing RUN continuation backslash, /mise not initialized before staging, env PATH schema forbids PATH → path_append) — each RCA'd and fixed in-tree; final tree has zero warnings (golangci-lint 0 issues, go vet clean).
- **R2 — no pre-existing/out-of-scope split:** every issue surfaced during the bed runs was fixed in the same tree; nothing parked.
- **R3 — no duplication:** the plugin renders its own stage (the four detection builders keep `kit.BuilderResolve`); the verb reuses `kit.DecodeInput` / `sdk.NewCheckContext` (the shared kit).
- **R4 — no workarounds:** pinned mise release, no curl-pipe, no sleeps/retries.
- **R5 — hard cutover:** new identifiers (builder:mise, verb:mise) grep-verified — no stale references to a former path; the repo is new, nothing to delete.
- **R6 — git safety:** feat/ branch, no force-push, PR-only landing, post-commit `git show --stat` verified.
- **R7 — gate proven:** unit tests + lint + the R10 bed above (fresh rebuild on a disposable target), all pasted.
- **R9 — artifact/binary integrity:** the R10 bed exercised the built image (check-image + check-live) on a disposable target; the built artifact is the one the checks ran against.
- **R10 — disposable-only, fresh-rebuild, coverage:** `check-mise-pod` is `disposable: true`; `charly check run` performed a fresh rebuild (`update` step) and re-ran check-live green; the bed ships the checks that prove the new functionality (mise-on-path, node-via-shims).

## Change classification

- Change class: new plugin repo (external, out-of-tree) + R10 bed
- Verification gate: unit tests + lint + R10 bed (pasted above)
- Attribution tier: fully tested and validated (R10 bed green end-to-end on a fresh rebuild; changed paths executed live)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
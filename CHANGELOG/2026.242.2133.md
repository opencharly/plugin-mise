# 2026.242.2133 — feat(beds): the check-mise-ubuntu-2604 R10 bed (Ubuntu 26.04 LTS)

## Summary

The **Ubuntu 26.04 LTS** mise test bed (mirroring the seven landed distro beds):

- **`ubuntu-2604-builder`** — the stage base: `base: ubuntu:26.04`, distro fallback list `[ubuntu-26.04, ubuntu]` (resolves to the stock `ubuntu` distro vocabulary — same fallback the ubuntu:24.04 bed uses), `build: [deb]`, `produce: [mise]`, composes `curl-tar`; ADE `plan:` with a deterministic toolchain check.
- **`check-mise-ubuntu-2604`** bed image + **`check-mise-ubuntu-2604-pod`** disposable pod (ADE `plan:` included), with the `distro:` declarations the bootstrap needs.
- The ubuntu:24.04 sibling's description gains the 24.04 qualifier for symmetry; its `plan:` is untouched.
- Entity names are **dot-free** (`ubuntu-2604`) — dots are reserved for dotted-path addressing in deploy-tree keys (the base image ref `ubuntu:26.04` keeps its dot).

The build-graph ordering defect (builder box must build BEFORE the consuming stage) is RCA'd and fixed upstream in opencharly/sdk#198 (`BoxNeedsBuilder` recognizes `mise.toml`/`.tool-versions` → the builder box enters the build order), landed via charly#490; this bed exercises that fix on the new distro.

## How tested

```
$ cd plugin-mise && charly box validate >/dev/null 2>&1; echo exit=$?
exit=0
```

```
$ charly check run check-mise-ubuntu-2604-pod
charly check run check-mise-ubuntu-2604-pod: PASS (steps=13)
```
(disposable: true, fresh rebuild from the FINAL committed tree `ce46fe8` — which carries the ADE plans added in review — image logs under `.check/check-mise-ubuntu-2604-pod/2026.242.21*/image-build.log`: the builder stage renders `FROM ubuntu-2604-builder`, installs mise, runs `mise install && mise reshim`; the pod plan proves mise on PATH + zig via the shims)

## Rulebook compliance

- **R1** — two live failures RCA'd before remediation: (1) the dotted entity name `ubuntu-26.04` in a deploy-tree key — dots are reserved for dotted-path addressing; renamed dot-free (`ubuntu-2604`). (2) review round 1 found the missing ADE `plan:` blocks and a sibling pod whose `plan:` my insert had absorbed; the plans are restored, and the bed was re-run green on the final tree.
- **R3** — the bed mirrors the existing distro beds; the consumer + fixture are shared.
- **R6** — feat/ branch, PR-only landing, corrections as added commits.
- **R7/R10** — gate proven by pasted `box validate` + the disposable fresh-rebuild bed PASS.

## Change classification

- **Change class:** feature (new distro test bed)
- **Verification gate:** `box validate` + the disposable fresh-rebuild bed PASS (pasted)
- **Attribution tier:** fully tested and validated

*Assisted-by: pi deepseek/deepseek-v4-flash-0731 (fully tested and validated)*